server:
  port: 8500

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gradeus?useSSL=false
    username: root
    password: Pass@1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect