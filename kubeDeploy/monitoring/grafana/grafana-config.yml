apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-config
  labels:
    app: grafana
data:
  grafana.ini: |
    [auth]
    disable_login_form = false

    [security]
    admin_user = admin
    admin_password = admin
